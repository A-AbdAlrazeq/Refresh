// 1. Declare variables
let name = "Abd Alrahman";
const age = 26;
console.log(`Hello, my name is ${name} and I am ${age} years old.`);

// 2. Create an array and loop through it
let numbers = [1, 2, 3, 4, 5];
numbers.forEach((num) => console.log(num * 2));

// 3. Write a function
function greet(user) {
  return `Hello, ${user}!`;
}
console.log(greet("Developer"));

// 4. Use an object
const person = {
  name: "Abd Alrahman",
  job: "Full Stack Developer",
};
console.log(`${person.name} works as a ${person.job}`);
